# Green Sharks — environment variables (optional)
# Copy to .env and fill in for local or Netlify/server use.
# Do not commit .env or real API keys.

# LLM / API (e.g. for future YouTube embed extraction or Discord bot)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

# Netlify serverless (if using functions that need env)
# Same names can be set in Netlify UI: Site settings → Environment variables
